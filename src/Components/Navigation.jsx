import { Link, useLocation } from 'react-router-dom';

function Navigation() {
  //conditionally renders page and highlights link with glow class depending on page location
  const currentPage = useLocation().pathname;

  return //TODO: complete Nav file
}

export default Navigation;