export default function Project(props) {
    const projects = props.data;
    // Maps through data from props passed through the portfolio page and to render a separate <div> for each.
    return //TODO: Complete Project section
};